{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Run QB64/QB64pe BASIC Program",
            "preLaunchTask": "${command:basic.runWithSelection}"
        }
    ]
}